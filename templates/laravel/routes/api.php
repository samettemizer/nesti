<?php

/*
|--------------------------------------------------------------------------
| API Routes
|--------------------------------------------------------------------------
|
| This surface intentionally starts empty. The default `install:api`
| `/api/user` route is removed so the OpenAPI gate begins from a fully
| documented, zero-route baseline.
|
| Every route added to this file MUST appear in the exported OpenAPI
| document (openapi.json). The pipeline regenerates that document and fails
| the attempt when a route here is missing from it.
|
*/
