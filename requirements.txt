anthropic>=0.25.0
requests>=2.31.0
python-dotenv>=1.0.0
docker>=7.0.0
gitpython>=3.1.40
python-gitlab>=4.4.0
redis>=5.0.0
langgraph>=0.2.0
langchain-core>=0.3.0
# Pinned below 2.0: the MCP SDK renamed FastMCP → MCPServer in 2.x and changed
# the decorator API. mcp_server/server.py builds on FastMCP, and its import
# order plus the __main__ delegation guard are load-bearing invariants, so the boring fix is to stay on the 1.x line.
mcp>=1.0.0,<2
